# Folow Him — Phase 2
Brand: **Folow Him** (one L). This is the first connected frontend for authentication, Today, Journal, and Prayers. Configure `.env.local` with the Supabase publishable key, then run `npm install && npm run dev`.
